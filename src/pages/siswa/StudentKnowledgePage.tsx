import { BookOpen, ExternalLink, Search } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { learningPlanStore, type LearningItem } from "@/data/learningPlanStore";
export default function StudentKnowledgePage() { const [items, setItems] = useState<LearningItem[]>([]); const [query, setQuery] = useState(""); useEffect(() => { setItems(learningPlanStore.list().filter((item) => item.type === "Materi" && item.status === "Diterbitkan")); return learningPlanStore.subscribe(() => setItems(learningPlanStore.list().filter((item) => item.type === "Materi" && item.status === "Diterbitkan"))); }, []); const shown = useMemo(() => items.filter((item) => `${item.title} ${item.className} ${item.description}`.toLowerCase().includes(query.toLowerCase())), [items, query]); return <div className="min-h-full bg-[hsl(var(--guru-sapphire-soft)/0.25)] p-4 sm:p-6"><div className="mx-auto max-w-6xl space-y-5"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[hsl(var(--guru-turquoise))]">Portal Siswa · UjiBetaversiMIrai</p><h1 className="mt-1 text-2xl font-black">Jendela Ilmu</h1><p className="mt-1 text-sm text-muted-foreground">Materi yang sudah diterbitkan guru untuk kelasmu.</p></div><div className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Cari materi..." className="w-full rounded-xl border border-input bg-card py-3 pl-10 pr-4 text-sm outline-none focus:border-primary"/></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{shown.map((item) => <article key={item.id} className="rounded-2xl border border-border bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-center justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[hsl(var(--guru-turquoise-soft))] text-[hsl(var(--guru-turquoise))]"><BookOpen size={19}/></span><span className="rounded-full bg-primary/10 px-2 py-1 text-[10px] font-semibold text-primary">{item.className}</span></div><h2 className="mt-4 font-bold">{item.title}</h2><p className="mt-1 text-xs leading-relaxed text-muted-foreground">{item.description || "Materi pembelajaran dari guru."}</p><button type="button" onClick={() => { if (item.url) window.open(item.url, "_blank", "noopener,noreferrer"); }} disabled={!item.url} className="mt-5 flex items-center gap-2 rounded-xl bg-primary px-3 py-2 text-xs font-bold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-40">Buka Materi <ExternalLink size={14}/></button></article>)}{!shown.length && <div className="col-span-full rounded-2xl border border-dashed border-border bg-card px-5 py-14 text-center text-sm text-muted-foreground">Belum ada materi yang diterbitkan guru.</div>}</div></div></div>; }

import { useState } from "react";
import { ArrowLeft, Check, CheckCircle2, ChevronRight, ClipboardCheck, HelpCircle, BookOpen, Target, Trophy, X } from "lucide-react";
import StudentMeetingContent from "@/components/shared/StudentMeetingContent";

type Topic = "Sudut" | "Garis-Garis Sejajar";
type Step = "presensi" | "topik" | "quiz" | "hasil";
interface Question { question: string; options: string[]; correct: number; }
const content: Record<Topic, { questions: Question[] }> = {
  "Sudut": { questions: [{ question: "Sudut yang besarnya kurang dari 90° disebut sudut...", options: ["Lancip", "Siku-siku", "Tumpul", "Refleks"], correct: 0 }, { question: "Dua garis berpotongan membentuk sudut 35°. Besar sudut bertolak belakangnya adalah...", options: ["35°", "55°", "90°", "145°"], correct: 0 }, { question: "Sebuah sudut siku-siku besarnya...", options: ["45°", "90°", "180°", "60°"], correct: 1 }] },
  "Garis-Garis Sejajar": { questions: [{ question: "Dua garis yang tidak pernah bertemu walaupun diperpanjang disebut garis...", options: ["Sejajar", "Berpotongan", "Tegak lurus", "Lengkung"], correct: 0 }, { question: "Garis yang memotong dua garis sejajar disebut garis...", options: ["Transversal", "Sejajar", "Berimpit", "Diagonal"], correct: 0 }, { question: "Pada dua garis sejajar yang dipotong garis transversal, sudut sehadap memiliki besar yang...", options: ["Sama", "Berjumlah 90°", "Berjumlah 180°", "Berbeda"], correct: 0 }] },
};

export default function RuangKelasAktif({ topic, studentName, onExit, onStartTutor }: { topic: Topic; studentName: string; onExit: () => void; onStartTutor: (topic: Topic) => void }) {
  const [step, setStep] = useState<Step>("presensi");
  const [questionIndex, setQuestionIndex] = useState(0);
  const [selected, setSelected] = useState<number | null>(null);
  const [answers, setAnswers] = useState<number[]>([]);
  const [revealed, setRevealed] = useState(false);
  const lesson = content[topic];
  const current = lesson.questions[questionIndex];
  const score = answers.filter((answer, index) => answer === lesson.questions[index].correct).length;
  const scorePercent = Math.round((score / lesson.questions.length) * 100);
  const assignedGroup = scorePercent < 50 ? "Tim Pengintai" : "Tim Navigator";
  const submit = () => { if (selected !== null) setRevealed(true); };
  const next = () => { const nextAnswers = [...answers, selected as number]; setAnswers(nextAnswers); setSelected(null); setRevealed(false); if (questionIndex + 1 < lesson.questions.length) setQuestionIndex((value) => value + 1); else setStep("hasil"); };
  return <div className="flex min-h-screen flex-col bg-background"><header className="flex items-center justify-between border-b border-border bg-card px-5 py-4"><div><p className="text-[10px] font-bold uppercase tracking-widest text-muted-foreground">Kelas Aktif · VII-A</p><p className="font-semibold">Matematika · {topic}</p></div><button onClick={onExit} className="flex items-center gap-1.5 rounded-full bg-muted px-3 py-1.5 text-xs text-muted-foreground"><ArrowLeft size={14}/> Keluar</button></header><div className="border-b border-border bg-card/60 px-5 py-4"><div className="mx-auto flex max-w-2xl items-center gap-2">{["presensi","topik","quiz","hasil"].map((item, index) => <div key={item} className="flex flex-1 items-center gap-2"><span className={`flex h-7 w-7 items-center justify-center rounded-full text-xs font-bold ${step === item ? "bg-primary text-primary-foreground" : ["presensi","topik","quiz","hasil"].indexOf(step) > index ? "bg-[hsl(var(--guru-turquoise))] text-primary-foreground" : "bg-muted text-muted-foreground"}`}>{["presensi","topik","quiz","hasil"].indexOf(step) > index ? <Check size={14}/> : index + 1}</span>{index < 3 && <span className="h-px flex-1 bg-border"/>}</div>)}</div></div><main className="flex flex-1 items-center justify-center p-5"><StudentMeetingContent /><div className="w-full max-w-xl">{step === "presensi" && <section className="text-center"><ClipboardCheck className="mx-auto mb-5 h-16 w-16 text-[hsl(var(--guru-turquoise))]"/><h1 className="text-3xl font-black">Presensi Kehadiran</h1><p className="mt-2 text-sm text-muted-foreground">Konfirmasi kehadiran di Matematika kelas VII-A.</p><div className="my-6 rounded-2xl border border-border bg-card p-5 text-left"><p className="flex justify-between text-sm"><span className="text-muted-foreground">Nama Siswa</span><b>{studentName}</b></p><p className="mt-3 flex justify-between text-sm"><span className="text-muted-foreground">Materi</span><b>{topic}</b></p><p className="mt-3 flex justify-between text-sm"><span className="text-muted-foreground">Status</span><b className="text-[hsl(var(--guru-turquoise))]">Hadir</b></p></div><button onClick={() => setStep("topik")} className="w-full rounded-2xl bg-[hsl(var(--guru-turquoise))] py-4 font-semibold text-primary-foreground">Konfirmasi Hadir</button></section>}{step === "topik" && <section className="text-center"><BookOpen className="mx-auto mb-5 h-16 w-16 text-[hsl(var(--guru-sapphire))]"/><h1 className="text-3xl font-black">{topic}</h1><p className="mt-2 text-sm text-muted-foreground">Topik Pembelajaran Kelas 7</p><button onClick={() => setStep("quiz")} className="mt-8 flex w-full items-center justify-center gap-2 rounded-2xl bg-primary py-4 font-semibold text-primary-foreground">Mulai Kuis <ChevronRight size={18}/></button></section>}{step === "quiz" && <section><div className="mb-5 flex items-center justify-between"><p className="flex items-center gap-2 text-sm font-bold"><HelpCircle size={17}/> Soal {questionIndex + 1}/{lesson.questions.length}</p><span className="text-xs text-muted-foreground">{topic}</span></div><div className="mb-4 rounded-2xl border border-border bg-card p-5"><p className="text-lg font-semibold leading-relaxed">{current.question}</p></div><div className="space-y-2.5">{current.options.map((option, index) => <button disabled={revealed} key={option} onClick={() => setSelected(index)} className={`flex w-full items-center gap-3 rounded-xl border p-3.5 text-left text-sm ${revealed && index === current.correct ? "border-[hsl(var(--guru-turquoise))] bg-[hsl(var(--guru-turquoise-soft))]" : revealed && selected === index ? "border-destructive bg-destructive/10" : selected === index ? "border-primary bg-primary/10" : "border-border bg-card hover:bg-muted"}`}><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-muted text-xs font-bold">{revealed && index === current.correct ? <Check size={15}/> : revealed && selected === index ? <X size={15}/> : String.fromCharCode(65 + index)}</span>{option}</button>)}</div>{!revealed ? <button disabled={selected === null} onClick={submit} className="mt-5 w-full rounded-2xl bg-primary py-4 font-semibold text-primary-foreground disabled:opacity-50">Konfirmasi Jawaban</button> : <div className="mt-4 rounded-2xl border border-border bg-card p-4"><p className={selected === current.correct ? "font-bold text-[hsl(var(--guru-turquoise))]" : "font-bold text-destructive"}>{selected === current.correct ? "Jawaban benar!" : `Belum tepat. Jawaban: ${current.options[current.correct]}`}</p><button onClick={next} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3 font-semibold text-primary-foreground">{questionIndex + 1 === lesson.questions.length ? "Lihat Hasil" : "Soal Berikutnya"}<ChevronRight size={16}/></button></div>}</section>}{step === "hasil" && <section className="text-center"><Trophy className="mx-auto h-20 w-20 text-[hsl(var(--guru-yellow))]"/><h1 className="mt-5 text-3xl font-black">Kuis Selesai</h1><p className="mt-2 text-muted-foreground">Nilai akhir: <b className="text-[hsl(var(--guru-turquoise))]">{scorePercent}</b>/100 · Jawaban benar: {score}/{lesson.questions.length}</p><div className={`mt-6 rounded-2xl border p-5 text-left ${assignedGroup === "Tim Navigator" ? "border-[hsl(var(--guru-sapphire)/0.3)] bg-[hsl(var(--guru-sapphire-soft))]" : "border-[hsl(var(--guru-yellow)/0.35)] bg-[hsl(var(--guru-yellow-soft))]"}`}><p className="font-bold">{assignedGroup}</p><p className="mt-1 text-sm text-muted-foreground">{assignedGroup === "Tim Navigator" ? "Nilai kamu di atas 50. Kamu siap menerima tantangan pengayaan." : "Nilai kamu di bawah 50. Kamu akan mendapat pendampingan untuk memperkuat pemahaman."}</p></div><div className="mt-6 rounded-2xl border border-border bg-card p-5 text-left"><p className="font-bold">Materi berikutnya</p><p className="mt-1 text-sm text-muted-foreground">Gunakan AI Tutor untuk {assignedGroup === "Tim Navigator" ? "memperdalam tantangan pengayaan" : "mendapatkan pendampingan dan latihan dasar"} pada topik {topic}.</p></div><button onClick={() => onStartTutor(topic)} className={`mt-5 flex w-full items-center justify-center gap-2 rounded-2xl py-4 font-semibold text-primary-foreground ${assignedGroup === "Tim Navigator" ? "bg-[hsl(var(--guru-sapphire))]" : "bg-[hsl(var(--guru-turquoise))]"}`}><Target size={18}/> {assignedGroup === "Tim Navigator" ? "Mulai Tantangan AI Tutor" : "Mulai Pendampingan AI Tutor"}</button><button onClick={onExit} className="mt-3 w-full rounded-2xl border border-border py-3 text-sm text-muted-foreground">Kembali ke Ruang Kelas</button></section>}</div></main></div>;
}

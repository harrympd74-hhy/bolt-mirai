import { CloudUpload, X } from "lucide-react";
import { useState } from "react";
import { learningPlanStore } from "@/data/learningPlanStore";
import { classMeetingStore } from "@/data/classMeetingStore";
export default function LearningContentModal({ onClose }: { onClose: () => void }) { const [title, setTitle] = useState(""); const [className, setClassName] = useState("VII-A"); const [type, setType] = useState("PDF Document"); const [access, setAccess] = useState("Bisa Diunduh"); const [description, setDescription] = useState(""); const [fileName, setFileName] = useState(""); const [meetingId, setMeetingId] = useState(classMeetingStore.list()[0]?.id || ""); const save = () => { if (!title.trim()) return; learningPlanStore.add({ meetingId, title, className, type: "Materi", status: "Draft", description: `${description}${fileName ? ` · File: ${fileName}` : ""}`, source: type }); onClose(); }; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-foreground/40 p-4"><div className="w-full max-w-md rounded-2xl border border-border bg-card p-5 shadow-2xl"><div className="flex items-center justify-between"><div><h2 className="font-bold">Unggah Materi Baru</h2><p className="mt-1 text-xs text-muted-foreground">UjiBetaversiMIrai · Materi guru</p></div><button type="button" onClick={onClose} aria-label="Tutup"><X size={18}/></button></div><label className="mt-4 flex cursor-pointer flex-col items-center rounded-xl border-2 border-dashed border-primary/30 bg-primary/5 px-4 py-7 text-center"><CloudUpload className="text-primary" size={28}/><span className="mt-2 text-xs font-semibold text-primary">Tarik dan lepas file di sini</span><span className="mt-1 text-[10px] text-muted-foreground">PDF, DOC, Video, Image</span><input type="file" accept=".pdf,.doc,.docx,.ppt,.pptx,.mp4,.jpg,.jpeg,.png" onChange={(event) => setFileName(event.target.files?.[0]?.name || "")} className="hidden"/></label>{fileName && <p className="mt-2 rounded-lg bg-muted px-3 py-2 text-xs">{fileName}</p>}<label className="mt-4 block text-xs font-semibold">Judul Materi<input value={title} onChange={(event) => setTitle(event.target.value)} className="mt-1 w-full rounded-xl border border-input bg-muted/30 px-3 py-2.5 text-sm"/></label><div className="mt-3 grid grid-cols-2 gap-3"><label className="text-xs font-semibold">Pilih Kelas<select value={className} onChange={(event) => setClassName(event.target.value)} className="mt-1 w-full rounded-xl border border-input bg-muted/30 px-3 py-2.5 text-sm"><option>VII-A</option><option>VII-B</option><option>VII-C</option></select></label><label className="text-xs font-semibold">Jenis File<select value={type} onChange={(event) => setType(event.target.value)} className="mt-1 w-full rounded-xl border border-input bg-muted/30 px-3 py-2.5 text-sm"><option>PDF Document</option><option>DOC/DOCX</option><option>PPT/PPTX</option><option>Video</option><option>Image</option></select></label></div><label className="mt-3 block text-xs font-semibold">Akses Siswa<select value={access} onChange={(event) => setAccess(event.target.value)} className="mt-1 w-full rounded-xl border border-input bg-muted/30 px-3 py-2.5 text-sm"><option>Bisa Diunduh</option><option>Lihat Saja</option></select></label><label className="mt-3 block text-xs font-semibold">Deskripsi Singkat<textarea value={description} onChange={(event) => setDescription(event.target.value)} rows={3} className="mt-1 w-full resize-none rounded-xl border border-input bg-muted/30 px-3 py-2.5 text-sm"/></label><button type="button" onClick={save} disabled={!title.trim()} className="mt-4 w-full rounded-xl bg-primary py-3 text-sm font-bold text-primary-foreground disabled:opacity-50">Simpan Materi</button></div></div>; }

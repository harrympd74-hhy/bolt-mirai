import { CalendarClock, UsersRound } from "lucide-react";
import { useEffect, useState } from "react";
import { classMeetingStore } from "@/data/classMeetingStore";
export default function ActiveClassWorkspace() { const [meetings, setMeetings] = useState(classMeetingStore.list()); useEffect(() => classMeetingStore.subscribe(() => setMeetings([...classMeetingStore.list()])), []); const active = meetings.filter((meeting) => meeting.isActive && meeting.status === "published"); return <div className="mx-auto max-w-6xl space-y-5"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[hsl(var(--guru-turquoise))]">Kelas Saya · Terhubung ke Siswa</p><h1 className="mt-1 text-2xl font-black">Kelas Aktif</h1><p className="mt-1 text-sm text-muted-foreground">Pertemuan yang sedang berlangsung dan dapat diakses siswa.</p></div>{active.length ? <div className="grid gap-4 md:grid-cols-2">{active.map((meeting) => <article key={meeting.id} className="rounded-2xl border border-[hsl(var(--guru-turquoise)/0.45)] bg-[hsl(var(--guru-turquoise-soft)/0.42)] p-5 shadow-sm"><div className="flex items-start justify-between"><span className="flex items-center gap-2 rounded-full bg-[hsl(var(--guru-turquoise))] px-3 py-1 text-xs font-bold text-primary-foreground"><span className="h-2 w-2 animate-pulse rounded-full bg-primary-foreground"/>Live untuk siswa</span><CalendarClock size={19} className="text-[hsl(var(--guru-turquoise))]"/></div><h2 className="mt-5 text-xl font-black">{meeting.title}</h2><p className="mt-1 text-sm text-muted-foreground">{meeting.className} · {new Date(meeting.startsAt).toLocaleTimeString("id-ID",{hour:"2-digit",minute:"2-digit"})}–{new Date(meeting.endsAt).toLocaleTimeString("id-ID",{hour:"2-digit",minute:"2-digit"})}</p><div className="mt-5 flex items-center gap-2 text-xs font-semibold text-[hsl(var(--guru-turquoise))]"><UsersRound size={15}/> Siswa dapat masuk melalui Ruang Kelas</div></article>)}</div> : <div className="rounded-2xl border border-dashed border-border bg-card p-12 text-center"><CalendarClock className="mx-auto text-muted-foreground" size={36}/><h2 className="mt-4 font-bold">Belum ada kelas aktif</h2><p className="mt-1 text-sm text-muted-foreground">Aktifkan pertemuan dari menu Pertemuan Kelas saat waktunya dimulai.</p></div>}</div>; }

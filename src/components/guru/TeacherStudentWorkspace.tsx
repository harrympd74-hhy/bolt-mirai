import { BarChart3, Search, UsersRound } from "lucide-react";
import { useMemo, useState } from "react";
import { betaGroups, betaStudents } from "@/data/teacherBetaversionData";
export default function TeacherStudentWorkspace({ section }: { section: string }) { const [query, setQuery] = useState(""); const list = useMemo(() => betaStudents.filter((student) => `${student.name} ${student.className} ${student.group}`.toLowerCase().includes(query.toLowerCase())), [query]); const titles: Record<string,string> = { "semua-siswa":"Semua Siswa", "progress-capaian":"Progress & Capaian", "kelompok-belajar":"Kelompok Belajar", "productive-struggle":"Productive Struggle", "tutor-sebaya":"Tutor Sebaya" }; const title = titles[section] || "Siswa"; return <div className="mx-auto max-w-6xl space-y-5"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[hsl(var(--guru-turquoise))]">UjiBetaversiMIrai · Siswa</p><h1 className="mt-1 text-2xl font-black">{title}</h1><p className="mt-1 text-sm text-muted-foreground">Data siswa terhubung dengan kelas dan konteks dasbor siswa.</p></div>{section === "kelompok-belajar" || section === "tutor-sebaya" ? <div className="grid gap-4 sm:grid-cols-2">{betaGroups.map((group) => <article key={group.name} className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[hsl(var(--guru-sapphire-soft))] text-primary"><UsersRound size={18}/></span><div><h2 className="font-bold">{group.name}</h2><p className="text-xs text-muted-foreground">{group.members} siswa · {group.progress}% progres</p></div></div><div className="mt-4 h-2 rounded-full bg-muted"><div className="h-full rounded-full bg-primary" style={{width:`${group.progress}%`}}/></div><button type="button" className="mt-4 text-xs font-bold text-primary">Buka kelompok</button></article>)}</div> : <><div className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Cari nama, kelas, atau kelompok..." className="w-full rounded-xl border border-input bg-card py-3 pl-10 pr-4 text-sm outline-none focus:border-primary"/></div><div className="overflow-x-auto rounded-2xl border border-border bg-card"><table className="min-w-[760px] w-full text-sm"><thead><tr className="border-b border-border bg-muted/50 text-left text-xs uppercase text-muted-foreground">{["Nama", "Kelas", "Progres", "Kehadiran", "Kelompok", section === "productive-struggle" ? "Struggle" : "Aksi"].map((heading) => <th key={heading} className="px-4 py-3">{heading}</th>)}</tr></thead><tbody className="divide-y divide-border">{list.map((student) => <tr key={student.id} className="hover:bg-muted/30"><td className="px-4 py-3 font-semibold">{student.name}<span className="block text-[10px] font-normal text-muted-foreground">{student.nisn}</span></td><td className="px-4 py-3">{student.className}</td><td className="px-4 py-3"><div className="flex items-center gap-2"><div className="h-2 w-20 rounded-full bg-muted"><div className="h-full rounded-full bg-[hsl(var(--guru-turquoise))]" style={{width:`${student.progress}%`}}/></div>{student.progress}%</div></td><td className="px-4 py-3">{student.attendance}%</td><td className="px-4 py-3">{student.group}</td><td className="px-4 py-3">{section === "productive-struggle" ? <span className="rounded-full bg-[hsl(var(--guru-yellow-soft))] px-2 py-1 text-xs font-bold text-[hsl(var(--guru-brown))]">{student.struggle}%</span> : <button type="button" className="flex items-center gap-1 text-xs font-bold text-primary"><BarChart3 size={14}/> Lihat detail</button>}</td></tr>)}{!list.length && <tr><td colSpan={6} className="px-4 py-10 text-center text-muted-foreground">Siswa tidak ditemukan.</td></tr>}</tbody></table></div></>}</div>; }

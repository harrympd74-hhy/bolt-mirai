import { FileText, Plus, Sparkles } from "lucide-react";
import { useEffect, useState } from "react";
import { learningPlanStore, type LearningItem } from "@/data/learningPlanStore";
import LearningContentModal from "./LearningContentModal";
export default function LearningPlanWorkspace({ section }: { section: string }) { const [items, setItems] = useState<LearningItem[]>(learningPlanStore.list()); const [modal, setModal] = useState(false); useEffect(() => learningPlanStore.subscribe(() => setItems([...learningPlanStore.list()])), []); const title = section === "rencana-pembelajaran" ? "Rencana Pembelajaran" : ({"materi-konten":"Materi & Konten", asesmen:"Asesmen (Awal & Akhir)", "ai-tutor":"AI Tutor MIRAI", "refleksi-siswa":"Refleksi Siswa", "aktivitas-kolaboratif":"Aktivitas Kolaboratif"} as Record<string,string>)[section] || "Pembelajaran"; const shown = section === "rencana-pembelajaran" ? items : items.filter((item) => section === "materi-konten" ? item.type === "Materi" : section === "asesmen" ? item.type === "Asesmen" : section === "aktivitas-kolaboratif" ? item.type === "Kolaboratif" : true); return <div className="mx-auto max-w-6xl space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[hsl(var(--guru-turquoise))]">UjiBetaversiMIrai · Pembelajaran</p><h1 className="mt-1 text-2xl font-black">{title}</h1><p className="mt-1 text-sm text-muted-foreground">Guru dapat mengisi, memperbarui, dan menyiapkan pembelajaran untuk siswa.</p></div><button type="button" onClick={() => setModal(true)} className="flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-xs font-bold text-primary-foreground"><Plus size={15}/> {section === "materi-konten" ? "Unggah Materi Baru" : "Tambah Data"}</button></div><div className="overflow-x-auto rounded-2xl border border-border bg-card"><table className="min-w-[820px] w-full text-sm"><thead><tr className="border-b border-border bg-muted/50 text-left text-xs uppercase text-muted-foreground">{["Judul", "Kelas", "Jenis", "Sumber", "Status", "Aksi"].map((label) => <th key={label} className="px-4 py-3">{label}</th>)}</tr></thead><tbody className="divide-y divide-border">{shown.map((item) => <tr key={item.id} className="hover:bg-muted/30"><td className="px-4 py-3 font-semibold"><span className="flex items-center gap-2"><FileText size={15} className="text-primary"/>{item.title}</span><span className="block pl-6 text-xs font-normal text-muted-foreground">{item.description}</span></td><td className="px-4 py-3">{item.className}</td><td className="px-4 py-3"><span className="rounded-full bg-primary/10 px-2 py-1 text-xs font-semibold text-primary">{item.type}</span></td><td className="px-4 py-3 text-muted-foreground">{item.source}</td><td className="px-4 py-3"><span className={item.status === "Diterbitkan" ? "text-[hsl(var(--guru-turquoise))]" : "text-muted-foreground"}>{item.status}</span></td><td className="px-4 py-3"><button type="button" className="flex items-center gap-1 text-xs font-bold text-primary"><Sparkles size={14}/> Edit</button></td></tr>)}{!shown.length && <tr><td colSpan={6} className="px-4 py-12 text-center text-muted-foreground">Belum ada data pada submenu ini.</td></tr>}</tbody></table></div>{modal && <LearningContentModal onClose={() => setModal(false)}/>}</div>; }

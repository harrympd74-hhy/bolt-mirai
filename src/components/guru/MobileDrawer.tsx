import { X } from "lucide-react";
import { navItems } from "./navConfig";
export default function MobileDrawer({open,onClose,activeItem,onNavigate}:{open:boolean;onClose:()=>void;activeItem:string;onNavigate:(id:string)=>void}){return <><button type="button" aria-label="Tutup menu" onClick={onClose} className={`fixed inset-0 z-50 bg-slate-950/50 transition lg:hidden ${open?"opacity-100":"pointer-events-none opacity-0"}`}/><aside className={`fixed inset-y-0 left-0 z-50 flex w-72 flex-col bg-[hsl(var(--mirai-sidebar))] transition-transform lg:hidden ${open?"translate-x-0":"-translate-x-full"}`}><div className="flex items-center justify-between border-b border-white/10 px-4 py-4"><span className="text-lg font-black tracking-widest text-white">MIRAI</span><button type="button" onClick={onClose} aria-label="Tutup"><X size={18} className="text-white"/></button></div><nav className="flex-1 overflow-y-auto py-3">{navItems.filter(item=>item.id!=="profil-guru").map(item=>{const Icon=item.icon;return <div key={item.id}>{item.section&&<p className="px-4 pb-1 pt-4 text-[10px] font-bold uppercase tracking-widest text-white/45">{item.section}</p>}<button type="button" onClick={()=>onNavigate(item.id)} className={`flex w-full items-center gap-3 border-l-2 px-4 py-2.5 text-left text-sm ${activeItem===item.id?"border-white bg-white/15 text-white":"border-transparent text-white/75 hover:bg-white/10"}`}><Icon size={16}/>{item.label}</button></div>})}</nav></aside></>}

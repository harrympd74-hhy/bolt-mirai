import { mobileBottomItems } from "./navConfig";
export default function MobileBottomNav({activeItem,onNavigate}:{activeItem:string;onNavigate:(id:string)=>void}){return <nav className="safe-area-pb fixed bottom-0 left-0 right-0 z-40 flex items-center justify-around border-t border-white/10 bg-[hsl(var(--mirai-sidebar))] lg:hidden">{mobileBottomItems.map(item=>{const Icon=item.icon;return <button type="button" key={item.id} onClick={()=>onNavigate(item.id)} className={`flex flex-1 flex-col items-center gap-0.5 px-2 py-2 ${activeItem===item.id?"text-white":"text-white/55"}`}><Icon size={18}/><span className="truncate text-[10px] font-medium">{item.label}</span></button>})}</nav>}

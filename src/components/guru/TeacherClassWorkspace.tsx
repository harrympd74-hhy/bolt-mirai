import { BookOpen, CalendarDays, ChevronRight, Plus, Users } from "lucide-react";
import { betaClasses } from "@/data/teacherBetaversionData";
export default function TeacherClassWorkspace({ section }: { section: string }) { const titles: Record<string,string> = { "daftar-kelas":"Daftar Kelas", "buat-kelas":"Buat Kelas Baru", "jadwal-kelas":"Jadwal Kelas", "arsip-kelas":"Arsip Kelas" }; const title = titles[section] || "Kelas Saya"; return <div className="mx-auto max-w-6xl space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[hsl(var(--guru-turquoise))]">UjiBetaversiMIrai · Kelas Saya</p><h1 className="mt-1 text-2xl font-black">{title}</h1><p className="mt-1 text-sm text-muted-foreground">Kelas, jadwal, dan siswa terhubung dalam satu workspace.</p></div><button type="button" className="flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-xs font-bold text-primary-foreground"><Plus size={15}/> {section === "buat-kelas" ? "Simpan Kelas" : "Tambah Kelas"}</button></div><div className="grid gap-4 lg:grid-cols-3">{betaClasses.map((item) => <article key={item.id} className="rounded-2xl border border-border bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between"><span className="rounded-lg bg-[hsl(var(--guru-turquoise-soft))] px-2.5 py-1 text-xs font-black text-[hsl(var(--guru-turquoise))]">{item.name}</span><Users size={17} className="text-muted-foreground"/></div><h2 className="mt-4 font-bold">{item.subject}</h2><p className="mt-1 text-xs text-muted-foreground">{item.schedule}</p><div className="mt-5 flex items-center justify-between text-xs"><span className="flex items-center gap-1 text-muted-foreground"><Users size={13}/>{item.students} siswa</span><b className="text-primary">{item.progress}% progres</b></div><div className="mt-2 h-2 rounded-full bg-muted"><div className="h-full rounded-full bg-[hsl(var(--guru-turquoise))]" style={{width:`${item.progress}%`}}/></div><button type="button" className="mt-4 flex items-center gap-1 text-xs font-bold text-primary">Buka kelas <ChevronRight size={14}/></button></article>)}</div><div className="grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border border-border bg-card p-5"><CalendarDays className="text-primary" size={20}/><h2 className="mt-3 font-bold">Jadwal terhubung</h2><p className="mt-1 text-sm text-muted-foreground">Pertemuan guru dan kartu kelas siswa memakai jadwal yang sama.</p></div><div className="rounded-2xl border border-border bg-card p-5"><BookOpen className="text-[hsl(var(--guru-yellow))]" size={20}/><h2 className="mt-3 font-bold">Materi aktif</h2><p className="mt-1 text-sm text-muted-foreground">Materi yang diterbitkan guru dapat muncul di ruang siswa sesuai kelas.</p></div></div></div>; }

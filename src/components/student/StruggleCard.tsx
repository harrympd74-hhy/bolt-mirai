import { ArrowRight, Search } from "lucide-react";
import { getStruggleLabel } from "@/lib/struggleScore";
export default function StruggleCard({ score = 62, onViewProcess }: { score?: number; onViewProcess?: () => void }) { const info = getStruggleLabel(score); const tone = info.tone === "mustard" ? "bg-[hsl(var(--guru-yellow))]" : info.tone === "purple" ? "bg-[hsl(267_55%_55%)]" : "bg-[hsl(var(--guru-turquoise))]"; return <section className="rounded-2xl border border-[hsl(var(--guru-yellow)/0.3)] bg-card p-5 shadow-sm"><div className="flex items-center gap-2"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[hsl(var(--guru-yellow-soft))] text-[hsl(var(--guru-brown))]"><Search size={17}/></span><div><h2 className="text-sm font-bold">Problem Struggle</h2><p className="text-[10px] text-muted-foreground">Proses belajar yang sedang kamu bangun</p></div></div><div className="mt-4 flex items-center justify-between text-sm font-bold"><span>{score}%</span><span className="text-xs font-semibold text-muted-foreground">{info.label}</span></div><div className="mt-2 h-3 overflow-hidden rounded-full bg-muted"><div className={`h-full rounded-full ${tone} transition-all`} style={{ width: `${score}%` }}/></div><p className="mt-3 text-xs leading-relaxed text-muted-foreground">AI Tutor memantau waktu pengerjaan, pola langkah berpikir, frekuensi bantuan, dan proses tanya-coba-refleksi.</p><button type="button" onClick={onViewProcess} className="mt-4 flex items-center gap-1 text-xs font-bold text-primary hover:gap-2">Lihat Proses Belajar <ArrowRight size={14}/></button></section>; }

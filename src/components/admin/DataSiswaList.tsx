import { useState } from "react";
import { BookOpen, Pencil, Trash2, X } from "lucide-react";
import { deleteSiswaRecord, getSiswaList, saveSiswaRecord, statusBadgeColor, type SiswaRecord } from "@/data/siswaStore";
export default function DataSiswaList({searchValue=""}:{searchValue?:string}){const [version,setVersion]=useState(0);const [selected,setSelected]=useState<SiswaRecord|null>(null);const list=getSiswaList().filter(s=>`${s.namaLengkap} ${s.kodeSiswa} ${s.kelas}`.toLowerCase().includes(searchValue.toLowerCase()));return <div className="rounded-2xl border border-border bg-card shadow-sm"><div className="border-b border-border p-5"><h3 className="font-bold">Data Siswa</h3><p className="text-sm text-muted-foreground">{list.length} siswa terdaftar</p></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b border-border bg-muted/50 text-left text-xs uppercase text-muted-foreground">{["Kode Siswa","Nama","NISN","Kelas","Status","Aksi"].map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody className="divide-y divide-border">{list.map(s=><tr key={s.kodeSiswa} className="hover:bg-muted/30"><td className="px-4 py-3 font-bold text-primary">{s.kodeSiswa}</td><td className="px-4 py-3 font-medium">{s.namaLengkap}</td><td className="px-4 py-3 text-muted-foreground">{s.nisn||"—"}</td><td className="px-4 py-3">{s.kelas||"—"}</td><td className="px-4 py-3"><span className={`rounded-full px-2 py-1 text-xs font-semibold ${statusBadgeColor[s.status]}`}>{s.status}</span></td><td className="px-4 py-3"><div className="flex gap-1"><button aria-label="Detail" onClick={()=>setSelected(s)} className="rounded-lg p-1.5 text-primary"><BookOpen className="h-4 w-4"/></button><button aria-label="Edit" onClick={()=>setSelected(s)} className="rounded-lg p-1.5 text-amber-500"><Pencil className="h-4 w-4"/></button><button aria-label="Hapus" onClick={()=>{if(confirm(`Hapus ${s.kodeSiswa}?`)){deleteSiswaRecord(s.kodeSiswa);setVersion(version+1)}}} className="rounded-lg p-1.5 text-destructive"><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody></table></div>{selected&&<StudentModal student={selected} onClose={()=>setSelected(null)} onSaved={()=>{setVersion(version+1);setSelected(null)}}/>}</div>}
function StudentModal({student,onClose,onSaved}:{student:SiswaRecord;onClose:()=>void;onSaved:()=>void}){const [name,setName]=useState(student.namaLengkap);return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/40 p-4"><div className="w-full max-w-lg rounded-2xl bg-card p-5 shadow-2xl"><div className="flex justify-between"><h3 className="font-bold">Detail / Edit Siswa</h3><button aria-label="Tutup" onClick={onClose}><X className="h-4 w-4"/></button></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><p>Kode<br/><b>{student.kodeSiswa}</b></p><label>Nama<input value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded-lg border border-input bg-muted/50 px-2 py-1"/></label><p>Kelas<br/><b>{student.kelas||"—"}</b></p><p>Status<br/><b>{student.status}</b></p></div><button onClick={()=>{saveSiswaRecord({...student,namaLengkap:name});onSaved()}} className="mt-5 w-full rounded-xl bg-primary py-2 text-primary-foreground">Simpan</button></div></div>}

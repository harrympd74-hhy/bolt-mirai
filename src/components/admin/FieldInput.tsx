import { SELECT_OPTS } from "./selectOpts";
export interface FieldInputProps { label:string; name:string; value:string; onChange:(key:string,value:string)=>void; type?:string; }
export function FieldInput({label,name,value,onChange,type="text"}:FieldInputProps){const opts=SELECT_OPTS[name];const cls="w-full rounded-lg border border-border bg-muted/50 px-3 py-2 text-sm text-foreground outline-none focus:border-primary focus:ring-2 focus:ring-ring/20";return <label className="block text-xs font-medium text-muted-foreground">{label}{opts?<select value={value||""} onChange={e=>onChange(name,e.target.value)} className={`${cls} mt-1`}>{opts.map(o=><option key={o} value={o}>{o||"—"}</option>)}</select>:<input type={type} value={value||""} onChange={e=>onChange(name,e.target.value)} placeholder={label} className={`${cls} mt-1 placeholder:text-muted-foreground`}/>}</label>}

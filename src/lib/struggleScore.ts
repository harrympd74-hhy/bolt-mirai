export type StruggleLabel = { label: string; tone: "green" | "lime" | "mustard" | "purple" };
export function calculateStruggleScore(data: { attempts: number; timeRatio: number; directionChanges: number; hints: number; question: number; encourage: number; giveTime: number; acknowledge: number }) { const attemptsScore = Math.min(data.attempts / 5, 1) * 100; const timeScore = Math.min(Math.max((data.timeRatio - 0.8) / 1.2, 0), 1) * 100; const inconsistencyScore = Math.min(data.directionChanges / 4, 1) * 100; const hintScore = Math.min(data.hints / 3, 1) * 100; const warshauerScore = (data.question + data.encourage + data.giveTime + data.acknowledge) / 4; return Math.round(attemptsScore * 0.25 + timeScore * 0.2 + inconsistencyScore * 0.2 + hintScore * 0.15 + warshauerScore * 0.2); }
export function getStruggleLabel(score: number): StruggleLabel { if (score <= 30) return { label: "Alur berpikir lancar", tone: "green" }; if (score <= 60) return { label: "Sedang membangun pemahaman", tone: "lime" }; if (score <= 85) return { label: "Perlu scaffolding tambahan", tone: "mustard" }; return { label: "Sedang dalam proses mendalam", tone: "purple" }; }
